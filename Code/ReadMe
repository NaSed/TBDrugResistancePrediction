This folde contains the whole code for preprocessing data as well as creating ML models.


DNA_Manipulation is a small package that helps to find mutation types, translate gene sequences, etc.
It can be used as 'import DNA_Manipulation.utils as DM' and then its functions called as 'DM.translate_TB_Gene(...)'.
